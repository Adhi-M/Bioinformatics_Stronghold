# The DNA strand from Rosalind goes here
dna_sequence = "XXX"

# We will build the complement sequence letter by letter
complement = ""

# Loop through each base to find its matching pair for base in dna_sequence 
for base in dna_sequence:
  if base == "A":
    complement += "T"
  elif base == "T":
    complement += "A"
  elif base == "C":
    complement += "G"
  elif base == "G":
    complement += "C"

# Reverse the string at the end to get the "reverse complement" 
reverse_complemnt = complement[::-1]

# Print the final result
print(reverse_complement)
